# OpenTracing with Python

Tutorial example for OpenTracing using Python (via branch).

```bash
make build
```

Then run it via docker

```bash
make run
# or
docker run -p 5000:5000 opentracing_flask_example:latest
```
